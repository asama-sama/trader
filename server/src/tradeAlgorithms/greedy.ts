// this algorithm uses greedy logic to figure out a trading strategy

import DataPoint from '../types/DataPoint'
import Trade from '../types/Trade'

const greedy = (data: DataPoint[]) : Trade[] => {
  return []
}

export default greedy
